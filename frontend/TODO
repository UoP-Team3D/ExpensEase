when claude is back:

add an information icon to pre-built categories
cancel account settings mid-setting change
handle errors in login properly (and anywhere else)
remove alerts everywhere
add mobile support to everything if possible